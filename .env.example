# ── Required ──────────────────────────────────────────────────────
# Google Gemini API key (get one at https://aistudio.google.com/apikey)
# Used by langchain-google-genai to call the Gemini LLM for agent reasoning.
GOOGLE_API_KEY=your_gemini_api_key_here

# ── Optional: LangSmith Observability ─────────────────────────────
# LangSmith provides tracing for every LLM call, tool invocation, and
# graph step. Extremely useful for debugging multi-agent workflows.
# Sign up at https://smith.langchain.com and create an API key.

# Set to "true" to enable trace logging to LangSmith.
LANGCHAIN_TRACING_V2=true

# Your LangSmith API key for authentication.
LANGCHAIN_API_KEY=your_langsmith_api_key_here

# Project name under which traces are grouped in the LangSmith dashboard.
LANGCHAIN_PROJECT=automaton-auditor
